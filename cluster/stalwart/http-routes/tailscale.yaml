apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tailscale
spec:
  hostnames:
    - mail.zarantonello.dev
  parentRefs:
    - name: srv-hetz-nbg-0-tailscale-https
      namespace: cilium
  rules:
    - backendRefs:
        - name: stalwart-http
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /
