apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: public
spec:
  hostnames:
    - mta-sts.zarantonello.dev
    - mta-sts.zarantonello.net
  parentRefs:
    - name: srv-hetz-nbg-0-public-https
      namespace: cilium
  rules:
    - backendRefs:
        - name: stalwart
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /.well-known/mta-sts.txt
