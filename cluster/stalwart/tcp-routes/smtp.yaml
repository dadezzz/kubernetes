apiVersion: gateway.networking.k8s.io/v1
kind: TCPRoute
metadata:
  name: smtp
spec:
  parentRefs:
    - name: srv-hetz-nbg-0-public-smtp
      namespace: cilium
  rules:
    - backendRefs:
        - name: stalwart
          port: 25
