apiVersion: v1
kind: Service
metadata:
  name: server
spec:
  ipFamilyPolicy: RequireDualStack
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    apps.zarantonello.dev/name: server
    apps.zarantonello.dev/namespace: umami
