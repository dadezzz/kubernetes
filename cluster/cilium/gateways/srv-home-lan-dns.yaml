apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: srv-home-lan-dns
spec:
  addresses:
    - type: IPAddress
      value: 192.168.1.4
  gatewayClassName: cilium
  infrastructure:
    annotations:
      lbipam.cilium.io/sharing-cross-namespace: "*"
      lbipam.cilium.io/sharing-key: srv-home-lan
  listeners:
    - allowedRoutes: &allowedRoutes
        namespaces:
          from: All
      name: dns-tcp
      port: 53
      protocol: TCP
    - allowedRoutes: *allowedRoutes
      name: dns-udp
      port: 53
      protocol: UDP
