apiVersion: v1
kind: PersistentVolume
metadata:
  name: forgejo-data
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  local:
    path: /var/mnt/data-0/forgejo-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - srv-0-thi-home
