apiVersion: apps/v1
kind: Deployment
metadata:
  name: forgejo
spec:
  selector:
    matchLabels:
      apps.zarantonello.dev/name: forgejo
      apps.zarantonello.dev/namespace: forgejo
  # Persistent volume cannot be read by 2 replicas.
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        monitoring.zarantonello.dev/logs: "true"
      labels:
        apps.zarantonello.dev/name: forgejo
        apps.zarantonello.dev/namespace: forgejo
    spec:
      containers:
        - envFrom:
            - configMapRef:
                name: forgejo-env
            - secretRef:
                name: forgejo-env
          image: codeberg.org/forgejo/forgejo:16.0.3-rootless@sha256:214f4ae63ee78be1e445e58573c88dc7215e72091210852e0df94eaac1a25685
          name: forgejo
          ports:
            - containerPort: 3000
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - mountPath: /var/lib/gitea/custom/conf
              name: config
            - mountPath: /var/lib/gitea/ssh
              name: signing-key
            - mountPath: /var/lib/gitea
              name: data
            - mountPath: /tmp
              name: tmp
      enableServiceLinks: false
      nodeSelector:
        kubernetes.io/hostname: srv-hetz-nbg-0
      securityContext:
        # Forgejo expects to run as user git.
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - emptyDir: {}
          name: tmp
        - emptyDir: {}
          name: config
        - name: data
          persistentVolumeClaim:
            claimName: data
        - name: signing-key
          secret:
            defaultMode: 0600
            secretName: forgejo-signing-key
