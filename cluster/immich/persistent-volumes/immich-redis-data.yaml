apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Mi
  local:
    path: /var/mnt/data-0/immich-redis-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - srv-0-thi-home
