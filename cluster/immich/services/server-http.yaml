apiVersion: v1
kind: Service
metadata:
  name: server-http
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    apps.zarantonello.dev/name: server
    apps.zarantonello.dev/namespace: immich
