apiVersion: v1
kind: PersistentVolume
metadata:
  name: llama-server-data
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Gi
  local:
    path: /var/mnt/data-0/llama-server-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - srv-home
