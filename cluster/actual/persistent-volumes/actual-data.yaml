apiVersion: v1
kind: PersistentVolume
metadata:
  name: actual-data
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Mi
  local:
    path: /var/mnt/data-0/actual-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - srv-hetz-nbg-0
