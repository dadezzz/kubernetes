apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: git-zarantonello-dev
spec:
  dnsNames:
    - git.zarantonello.dev
  issuerRef:
    kind: ClusterIssuer
    name: acme-cloudflare-dns
  secretName: git-zarantonello-dev-tls
